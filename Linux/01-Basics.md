# Linux Basics

## What is Linux ?
Linux is a kernel which manages hardware, memory, process and system calls.

## Basic Commands
- `pwd` : print work directory
- `ls` : list
- `cd` : current directory
- `touch` : create file
- `rm` : remove file
- `mkdir` : make directory
- `rmdir` : remove directory
- `man` : manual
